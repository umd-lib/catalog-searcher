[project]
name = "catalog-searcher"
version = "2.0.0-dev"
dependencies = [
    "cql-parser",
    "environs",
    "flask",
    "furl",
    "paste",
    "pymods",
    "requests",
    "uritemplate",
    "waitress",
]

[project.optional-dependencies]
test = [
    "httpretty",
    "pytest",
    "pytest-cov",
    "pytest-datadir",
]
